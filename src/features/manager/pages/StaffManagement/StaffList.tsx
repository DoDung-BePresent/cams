import { Typography } from 'antd';

const { Title } = Typography;

export const StaffList = () => {
  return (
    <div>
      <Title level={2}>Staff Management</Title>
      <p>Staff management page will be implemented next...</p>
    </div>
  );
};

export default StaffList;
