# Test Cases – Device Pairing

> **Module:** Device Pairing (Space ↔ Tablet)
> **ID Format:** `[WEB]:PAIR-NN`
> **Roles:** BrandManager (BM), StoreManager (SM), SystemAdmin (SA), PlaybackDevice (PD)
> **Columns:** Test Case ID | Test Case Description | Test Case Procedure | Expected Results | Pre-conditions

---

## Group 1 – Open Pair Device Modal (Manager Side)

| Test Case ID  | Test Case Description                                          | Test Case Procedure                                                                                                                                   | Expected Results                                                                                                                                                                                                                 | Pre-conditions                                      |
| ------------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [WEB]:PAIR-01 | BrandManager opens Device Pairing modal                        | 1. Log in as BrandManager. 2. Navigate to Space Management. 3. Click the "Pair Device" action for a space (or locate the pair icon in the space row). | The Device Pairing modal opens. Title shows "Device Pairing". Modal width is large. A loading spinner is shown briefly while fetching pair info.                                                                                 | BrandManager with an accessible space in own brand. |
| [WEB]:PAIR-02 | StoreManager opens Device Pairing modal                        | 1. Log in as StoreManager. 2. Navigate to Space Management. 3. Click "Pair Device" for a space.                                                       | Device Pairing modal opens showing current state of that space (paired device info or code generation section).                                                                                                                  | StoreManager with an accessible space in own store. |
| [WEB]:PAIR-03 | Modal shows "No Device Paired" alert when space has no device  | 1. Open Device Pairing modal for a space with no paired device.                                                                                       | An info Alert "No Device Paired" is displayed in the modal. The "Pair Code" section is visible below it.                                                                                                                         | Space has no paired device.                         |
| [WEB]:PAIR-04 | Modal auto-generates pair code when space has no device        | 1. Open Device Pairing modal for a space with no paired device and no active code.                                                                    | The pair code is auto-generated immediately after the modal opens. A 6-character code in "ABC-123" format is displayed. A yellow warning tag shows "Expires in: MM:SS".                                                          | Space has no paired device and no pending code.     |
| [WEB]:PAIR-05 | Modal shows paired device info when space already has a device | 1. Open Device Pairing modal for a space with a currently paired device.                                                                              | A "Paired Device" card is shown with fields: Manufacturer, Model, OS Version, App Version, Paired At (formatted date), Last Active (relative time as green tag). An "Unpair" button is visible. The Pair Code section is hidden. | Space has an active paired device.                  |
| [WEB]:PAIR-06 | Paired device "Last Active" shown as relative time             | 1. Open Device Pairing modal for a paired space. 2. Observe "Last Active" field.                                                                      | "Last Active" is displayed as a green success Tag with a relative time string (e.g., "5 minutes ago", "2 hours ago").                                                                                                            | Space has a paired device that was recently active. |
| [WEB]:PAIR-07 | Paired device fields show dash when metadata is missing        | 1. Open Device Pairing modal for a paired space where device metadata (manufacturer, model, etc.) was not submitted.                                  | Fields such as Manufacturer, Model, OS Version, App Version show "-" instead of blank.                                                                                                                                           | Device was paired without optional metadata fields. |
| [WEB]:PAIR-08 | Close button closes the modal                                  | 1. Open Device Pairing modal. 2. Click the "Close" button in the footer.                                                                              | Modal closes. The space list is unchanged.                                                                                                                                                                                       | Device Pairing modal is open.                       |

---

## Group 2 – Generate Pair Code

| Test Case ID  | Test Case Description                                           | Test Case Procedure                                                                                                     | Expected Results                                                                                                                                                                       | Pre-conditions                                  |
| ------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| [WEB]:PAIR-09 | Generate pair code manually                                     | 1. Open Device Pairing modal for a space with no device. 2. If no code is shown, click "Generate Pair Code" button.     | API calls `POST /api/cams/spaces/{spaceId}/pair-code`. A 6-character code in "ABC-123" display format appears in the center of a card. A yellow warning tag shows "Expires in: 15:00". | Space has no paired device. BM or SM logged in. |
| [WEB]:PAIR-10 | Pair code displayed in "ABC-123" format with dash               | 1. Generate a pair code.                                                                                                | The displayed code includes a hyphen in the middle (e.g., "EDV-SHT"). The code is shown in a large monospace font.                                                                     | Device Pairing modal open, code generated.      |
| [WEB]:PAIR-11 | Countdown timer starts at 15:00                                 | 1. Generate a pair code. 2. Observe the countdown tag immediately after generation.                                     | Timer reads "Expires in: 15:00" initially (or close to it).                                                                                                                            | Pair code just generated.                       |
| [WEB]:PAIR-12 | Countdown timer decrements every second                         | 1. Generate a pair code. 2. Wait 5 seconds while observing the timer.                                                   | Timer decreases by 1 second every second (e.g., 14:55, 14:54, 14:53...).                                                                                                               | Pair code is active.                            |
| [WEB]:PAIR-13 | Pair code disappears after timer reaches 00:00                  | 1. Generate a pair code (or use an existing near-expiry code). 2. Wait until the countdown reaches 00:00.               | The pair code display is cleared. The "Generate Pair Code" button reappears, prompting the manager to generate a new code.                                                             | Pair code is about to expire.                   |
| [WEB]:PAIR-14 | Generating a new code while one is active replaces the old code | 1. Generate a pair code (Code A). 2. Before it expires, click "Generate Pair Code" again (or "Revoke" then "Generate"). | The old code A is replaced by a new code B. The countdown resets to 15:00.                                                                                                             | Active pair code exists.                        |
| [WEB]:PAIR-15 | Generate Pair Code button shows loading state                   | 1. Click "Generate Pair Code" button.                                                                                   | Button enters loading state while the API request is in progress.                                                                                                                      | Device Pairing modal open, no active code.      |
| [WEB]:PAIR-16 | Error message shown if code generation fails                    | 1. Simulate a server error on `POST /api/cams/spaces/{spaceId}/pair-code`. 2. Click "Generate Pair Code".               | An error Alert is shown inside the Pair Code section with the server's error message (or fallback "Unable to generate pair code.").                                                    | Space accessible, server returns error.         |
| [WEB]:PAIR-17 | Error clears on next successful generation                      | 1. Generate code fails (error shown). 2. Retry and generation succeeds.                                                 | Error alert disappears. Pair code is displayed normally.                                                                                                                               | Previous code generation failed.                |
| [WEB]:PAIR-18 | Instruction text shown alongside pair code                      | 1. Generate a pair code.                                                                                                | Below/above the code display, text reads "Enter this code on the tablet:" and a description that the code expires in 15 minutes.                                                       | Pair code is active.                            |

---

## Group 3 – Revoke Pair Code

| Test Case ID  | Test Case Description                             | Test Case Procedure                                                                                                 | Expected Results                                                                                                         | Pre-conditions                                     |
| ------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| [WEB]:PAIR-19 | BrandManager revokes active pair code             | 1. Open Device Pairing modal with an active code. 2. Click the "Revoke Code" button (danger-styled).                | API calls `DELETE /api/cams/spaces/{spaceId}/pair-code`. Code display disappears. "Generate Pair Code" button reappears. | Pair code is currently active. BM or SM logged in. |
| [WEB]:PAIR-20 | Revoke Code button shows loading state            | 1. Click "Revoke Code".                                                                                             | Button enters loading state while request is pending.                                                                    | Active pair code displayed.                        |
| [WEB]:PAIR-21 | Revoked code can no longer be used by tablet      | 1. Generate a code. 2. Note the code value. 3. Revoke the code. 4. Attempt to pair a device using the revoked code. | The `POST /api/auth/pair` call returns 400 (invalid/expired code). Device is not paired.                                 | Pair code generated and then revoked.              |
| [WEB]:PAIR-22 | Revoke Code button only shown when code is active | 1. Open Device Pairing modal with no code generated. 2. Observe.                                                    | "Revoke Code" button is NOT visible. Only "Generate Pair Code" button is visible.                                        | No pair code is currently active.                  |

---

## Group 4 – Unpair Device

| Test Case ID  | Test Case Description                                   | Test Case Procedure                                                                      | Expected Results                                                                                                                                                                                                           | Pre-conditions                                  |
| ------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| [WEB]:PAIR-23 | BrandManager unpairs a device – confirmation dialog     | 1. Open Device Pairing modal for a space with a paired device. 2. Click "Unpair" button. | A confirmation modal appears with title "Unpair Device", message "Are you sure you want to unpair this device? The device will need to pair again to continue playback.", and "Yes, Unpair" (danger) and "Cancel" buttons. | Space has a paired device. BM or SM logged in.  |
| [WEB]:PAIR-24 | Confirm unpair removes device from space                | 1. Open confirm unpair dialog. 2. Click "Yes, Unpair".                                   | API calls `DELETE /api/cams/spaces/{spaceId}/unpair`. Modal updates to show "No Device Paired" alert. Paired device card disappears. "Generate Pair Code" section reappears.                                               | Space has a paired device, confirm dialog open. |
| [WEB]:PAIR-25 | Cancel unpair preserves device pairing                  | 1. Open confirm unpair dialog. 2. Click "Cancel".                                        | Dialog closes. Paired device card remains visible. No API call is made.                                                                                                                                                    | Unpair confirmation dialog is open.             |
| [WEB]:PAIR-26 | Unpair button shows loading state                       | 1. Click "Unpair" and confirm.                                                           | "Yes, Unpair" (or the Unpair button) shows a loading spinner while the API processes the request.                                                                                                                          | Space has a paired device.                      |
| [WEB]:PAIR-27 | StoreManager can unpair a device in own store's space   | 1. Log in as StoreManager. 2. Open Device Pairing modal. 3. Unpair device and confirm.   | Device is successfully unpaired. Modal shows "No Device Paired" state.                                                                                                                                                     | StoreManager with a paired space.               |
| [WEB]:PAIR-28 | After unpair, device loses access on next token refresh | 1. Unpair a device. 2. Device attempts `POST /api/auth/device/refresh-token`.            | Refresh returns 401. The tablet application receives an unauthorized error and must show the re-pair screen.                                                                                                               | Device was paired and is now unpaired.          |

---

## Group 5 – Device Pairing (Tablet / API Side)

| Test Case ID  | Test Case Description                                   | Test Case Procedure                                                                                                                                                                                  | Expected Results                                                                                                                                                                                                                        | Pre-conditions                                                         |
| ------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| [WEB]:PAIR-29 | Tablet pairs successfully with valid code               | 1. Manager generates pair code "ABC123" for Space X. 2. Call `POST /api/auth/pair` with `{ "code": "ABC123" }`.                                                                                      | Response 200 with `{ isSuccess: true, data: { deviceAccessToken, deviceRefreshToken, spaceId, spaceName, storeId, storeName, accessTokenExpiresAt } }`.                                                                                 | Valid active pair code exists for Space X.                             |
| [WEB]:PAIR-30 | Pair response contains all required fields              | 1. Pair a device with a valid code. 2. Inspect the response body.                                                                                                                                    | Response `data` object contains: `deviceAccessToken` (JWT string), `deviceRefreshToken` (opaque token), `spaceId` (GUID), `spaceName` (string), `storeId` (GUID), `storeName` (string), `accessTokenExpiresAt` (ISO 8601 UTC datetime). | Valid pair code used.                                                  |
| [WEB]:PAIR-31 | Pair with optional device metadata                      | 1. Call `POST /api/auth/pair` with full body: `{ "code": "ABC123", "manufacturer": "Samsung", "model": "SM-T510", "osVersion": "Android 14", "appVersion": "1.0.0", "deviceId": "android-id-123" }`. | Response 200 success. Manager's Device Pairing modal subsequently shows the correct manufacturer, model, OS, and app version.                                                                                                           | Valid pair code exists.                                                |
| [WEB]:PAIR-32 | Pair without optional metadata succeeds                 | 1. Call `POST /api/auth/pair` with only `{ "code": "ABC123" }`.                                                                                                                                      | Response 200 success. Device is paired. Optional fields (manufacturer, model, etc.) show as null in the manager's pair info view.                                                                                                       | Valid pair code exists.                                                |
| [WEB]:PAIR-33 | Pair with invalid (wrong) code returns 400              | 1. Call `POST /api/auth/pair` with `{ "code": "XXXXXX" }` (code that was never generated).                                                                                                           | Response 400. `isSuccess: false`. Error message indicates the code is invalid or does not exist.                                                                                                                                        | No active pair code matching "XXXXXX".                                 |
| [WEB]:PAIR-34 | Pair with expired code returns 400                      | 1. Generate a pair code. 2. Wait 15 minutes for it to expire (or simulate expiry). 3. Call `POST /api/auth/pair` with the expired code.                                                              | Response 400. `isSuccess: false`. Error indicates the code has expired. Device not paired.                                                                                                                                              | Pair code has expired.                                                 |
| [WEB]:PAIR-35 | Pair code input is case-insensitive                     | 1. Manager generates code displayed as "ABC-123". 2. Call `POST /api/auth/pair` with `{ "code": "abc123" }` (lowercase).                                                                             | Response 200 success. Code comparison is case-insensitive. Device is paired.                                                                                                                                                            | Active pair code "ABC123" exists.                                      |
| [WEB]:PAIR-36 | Pair code input strips hyphens                          | 1. Manager generates code displayed as "EDV-SHT". 2. Call `POST /api/auth/pair` with `{ "code": "EDV-SHT" }` (with hyphen).                                                                          | Response 200 success. Server strips the hyphen and matches "EDVSHT". Device is paired.                                                                                                                                                  | Active pair code "EDVSHT" exists.                                      |
| [WEB]:PAIR-37 | Cannot pair if space already has a paired device        | 1. Space X already has Device A paired. 2. Call `POST /api/auth/pair` with a new code generated for Space X.                                                                                         | Response 400 with `BusinessRuleViolation`. Message indicates the space already has an active paired device. Device B is not paired.                                                                                                     | Space X has an active paired device. A second pair code was generated. |
| [WEB]:PAIR-38 | Cannot pair to an inactive space                        | 1. Space Y has status = Inactive. 2. Generate a pair code for Space Y (if possible). 3. Call `POST /api/auth/pair`.                                                                                  | Response 400 or 404. `isSuccess: false`. Message indicates the space or its parent store/brand is inactive.                                                                                                                             | Space Y has Inactive status.                                           |
| [WEB]:PAIR-39 | Cannot pair to a space in an inactive store             | 1. Store has status = Inactive. 2. Attempt to pair to one of its spaces.                                                                                                                             | Response 400 or 404. Device is not paired.                                                                                                                                                                                              | Parent store is inactive.                                              |
| [WEB]:PAIR-40 | Cannot pair to a space in an inactive brand             | 1. Brand has status = Inactive. 2. Attempt to pair to a space of that brand.                                                                                                                         | Response 400 or 404. Device is not paired.                                                                                                                                                                                              | Parent brand is inactive.                                              |
| [WEB]:PAIR-41 | deviceAccessToken is valid JWT with PlaybackDevice role | 1. Pair a device and receive `deviceAccessToken`. 2. Decode the JWT payload.                                                                                                                         | JWT claims include: role value = 3 (PlaybackDevice), spaceId, storeId. Token expiry (`exp`) is approximately 15 minutes from issue time.                                                                                                | Device paired successfully.                                            |

---

## Group 6 – Device Token Refresh

| Test Case ID  | Test Case Description                                  | Test Case Procedure                                                                                                                                                                       | Expected Results                                                                                                  | Pre-conditions                                  |
| ------------- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| [WEB]:PAIR-42 | Device refreshes token successfully before expiry      | 1. Pair a device, receive `deviceRefreshToken`. 2. Call `POST /api/auth/device/refresh-token` with `{ "deviceRefreshToken": "<token>" }` and `Authorization: Bearer <deviceAccessToken>`. | Response 200. New `deviceAccessToken` returned with an updated `expiresAt`. Device can continue making API calls. | Device is paired and refresh token is valid.    |
| [WEB]:PAIR-43 | Refresh response returns new access token and expiry   | 1. Call `POST /api/auth/device/refresh-token`. 2. Inspect the response body.                                                                                                              | Response `data` contains: `deviceAccessToken` (new JWT), `expiresAt` (new UTC expiry ~15 min from now).           | Valid device session exists.                    |
| [WEB]:PAIR-44 | Device token refresh fails after manager unpairs (401) | 1. Pair a device. 2. Manager unpairs the device. 3. Device calls `POST /api/auth/device/refresh-token` with old `deviceRefreshToken`.                                                     | Response 401. `isSuccess: false`. Indicates session has been revoked. Device must show the re-pair screen.        | Device was paired and then unpaired by manager. |
| [WEB]:PAIR-45 | Device token refresh fails if device itself unpairedv  | 1. Device calls `DELETE /api/cams/spaces/{spaceId}/unpair` to self-unpair. 2. Device then calls refresh token endpoint.                                                                   | Response 401. Session revoked. Device must re-pair.                                                               | Device is self-unpairing.                       |
| [WEB]:PAIR-46 | Refreshed device token still has PlaybackDevice role   | 1. Refresh device token. 2. Decode new JWT.                                                                                                                                               | Role claim = 3 (PlaybackDevice). spaceId and storeId claims preserved.                                            | Token refreshed successfully.                   |

---

## Group 7 – Paired Device API Access

| Test Case ID  | Test Case Description                                                | Test Case Procedure                                                                          | Expected Results                                                                                                   | Pre-conditions      |
| ------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------- |
| [WEB]:PAIR-47 | Paired device can get own pair info                                  | 1. Pair a device. 2. Call `GET /api/cams/spaces/pair-device` with device's Bearer token.     | Response 200 with `PairDeviceInfoResponse` containing the device's own session details (spaceId, deviceSessionId). | Device is paired.   |
| [WEB]:PAIR-48 | Paired device can self-unpair                                        | 1. Paired device calls `DELETE /api/cams/spaces/{spaceId}/unpair` with its own device token. | Response 200. Device session is revoked. Subsequent API calls with the old token return 401.                       | Device is paired.   |
| [WEB]:PAIR-49 | Paired device cannot call generate-code endpoint                     | 1. Paired device calls `POST /api/cams/spaces/{spaceId}/pair-code` with its device token.    | Response 403 Forbidden. Device role (PlaybackDevice) is not authorized to generate codes.                          | Device is paired.   |
| [WEB]:PAIR-50 | Unpaired / unauthenticated request to pair-code endpoint is rejected | 1. Call `POST /api/cams/spaces/{spaceId}/pair-code` without any Authorization header.        | Response 401. No pair code is generated.                                                                           | No auth in request. |

---

## Group 8 – Role-Based Access Control

| Test Case ID  | Test Case Description                                        | Test Case Procedure                                                                                     | Expected Results                                                                              | Pre-conditions                                         |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| [WEB]:PAIR-51 | BrandManager can generate code for own brand's space         | 1. Log in as BrandManager of Brand A. 2. Call `POST /api/cams/spaces/{spaceIdInBrandA}/pair-code`.      | Response 200 success. Pair code returned.                                                     | BrandManager owns Brand A. SpaceId belongs to Brand A. |
| [WEB]:PAIR-52 | BrandManager cannot generate code for another brand's space  | 1. Log in as BrandManager of Brand A. 2. Call `POST /api/cams/spaces/{spaceIdInBrandB}/pair-code`.      | Response 403 Forbidden. `isSuccess: false`. No code generated.                                | SpaceId belongs to a different brand.                  |
| [WEB]:PAIR-53 | StoreManager can generate code for own store's space         | 1. Log in as StoreManager of Store X. 2. Call `POST /api/cams/spaces/{spaceInStoreX}/pair-code`.        | Response 200 success. Pair code returned.                                                     | StoreManager owns Store X. SpaceId belongs to Store X. |
| [WEB]:PAIR-54 | StoreManager cannot generate code for another store's space  | 1. Log in as StoreManager of Store X. 2. Call `POST /api/cams/spaces/{spaceInStoreY}/pair-code`.        | Response 403 Forbidden. No code generated.                                                    | SpaceId belongs to a different store.                  |
| [WEB]:PAIR-55 | SystemAdmin cannot generate pair code                        | 1. Log in as SystemAdmin. 2. Attempt to open Device Pairing modal or call the generate-code API.        | If UI: "Pair Device" action is not available for SystemAdmin. If API: Response 403 Forbidden. | SystemAdmin account is active.                         |
| [WEB]:PAIR-56 | SystemAdmin cannot revoke pair code                          | 1. Log in as SystemAdmin. 2. Call `DELETE /api/cams/spaces/{spaceId}/pair-code`.                        | Response 403 Forbidden.                                                                       | SystemAdmin account. Active pair code exists.          |
| [WEB]:PAIR-57 | SystemAdmin cannot unpair a device                           | 1. Log in as SystemAdmin. 2. Call `DELETE /api/cams/spaces/{spaceId}/unpair`.                           | Response 403 Forbidden. Device remains paired.                                                | SystemAdmin account. Space has a paired device.        |
| [WEB]:PAIR-58 | BrandManager can view pair device info for own brand's space | 1. Log in as BrandManager. 2. Call `GET /api/cams/spaces/{spaceId}/pair-device`.                        | Response 200 with device session info.                                                        | BrandManager with access to that space.                |
| [WEB]:PAIR-59 | BrandManager can unpair device in own brand's space          | 1. Log in as BrandManager. 2. Call `DELETE /api/cams/spaces/{spaceId}/unpair`.                          | Response 200. Device unpaired.                                                                | Space in own brand has a paired device.                |
| [WEB]:PAIR-60 | Unauthenticated user cannot access any pairing endpoint      | 1. Call any pairing endpoint (`/pair-code`, `/unpair`, `/pair-device`) without an Authorization header. | Response 401 for all protected endpoints.                                                     | No auth token provided.                                |

---

## Group 9 – API Response Contract

| Test Case ID  | Test Case Description                                                       | Test Case Procedure                                             | Expected Results                                                                                                                                                                | Pre-conditions                         |
| ------------- | --------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| [WEB]:PAIR-61 | POST /api/cams/spaces/{spaceId}/pair-code response structure                | 1. BM/SM calls generate pair code endpoint.                     | Response: `{ isSuccess: true, data: { displayCode: "ABC-123", expiresAt: "<ISO8601 UTC>" } }`. HTTP 200 or 201.                                                                 | Valid BM/SM auth. Active space.        |
| [WEB]:PAIR-62 | DELETE /api/cams/spaces/{spaceId}/pair-code response structure              | 1. BM/SM calls revoke code endpoint.                            | Response: `{ isSuccess: true, message: "..." }`. HTTP 200.                                                                                                                      | Active pair code. BM/SM auth.          |
| [WEB]:PAIR-63 | GET /api/cams/spaces/{spaceId}/pair-device response structure – no device   | 1. Call get pair device info for a space with no paired device. | Response: `{ isSuccess: true, data: { deviceSessionId: null, manufacturer: null, model: null, osVersion: null, appVersion: null, pairedAtUtc: null, lastActiveAtUtc: null } }`. | Space has no paired device.            |
| [WEB]:PAIR-64 | GET /api/cams/spaces/{spaceId}/pair-device response structure – with device | 1. Call get pair device info for a space with a paired device.  | Response: `{ isSuccess: true, data: { deviceSessionId: "<non-null GUID>", manufacturer, model, osVersion, appVersion, pairedAtUtc, lastActiveAtUtc } }` with non-null values.   | Space has a paired device.             |
| [WEB]:PAIR-65 | DELETE /api/cams/spaces/{spaceId}/unpair response structure                 | 1. BM/SM calls unpair endpoint.                                 | Response: `{ isSuccess: true, message: "..." }`. HTTP 200.                                                                                                                      | Space has a paired device. BM/SM auth. |
| [WEB]:PAIR-66 | POST /api/auth/pair invalid code response structure                         | 1. Call pair endpoint with wrong code.                          | Response: `{ isSuccess: false, errorCode: "...", message: "..." }`. HTTP 400.                                                                                                   | Invalid code used.                     |
| [WEB]:PAIR-67 | POST /api/auth/device/refresh-token revoked session response                | 1. Call refresh-token after device has been unpaired.           | Response: `{ isSuccess: false, errorCode: "Unauthorized" or equivalent }`. HTTP 401.                                                                                            | Device session revoked.                |
