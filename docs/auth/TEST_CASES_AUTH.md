# Authentication Test Cases

> Format: Tab-separated — paste directly into Excel (each `|` column maps to one cell).
> Columns: **Test Case ID** | **Test Case Description** | **Test Case Procedure** | **Expected Results** | **Pre-conditions**

---

## Login

| Test Case ID | Test Case Description                                | Test Case Procedure                                                                              | Expected Results                                                                                                                    | Pre-conditions                                 |
| ------------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| AUTH-01      | Login succeeds with valid credentials (SystemAdmin)  | 1. Open login screen. 2. Enter valid email & password. 3. Click Login.                           | User is authenticated. Dashboard is displayed. Access token is stored.                                                              | Active SystemAdmin account exists.             |
| AUTH-02      | Login succeeds with valid credentials (BrandManager) | 1. Open login screen. 2. Enter valid BrandManager email & password. 3. Click Login.              | User is authenticated. Brand dashboard is displayed.                                                                                | Active BrandManager account exists.            |
| AUTH-03      | Login succeeds with valid credentials (StoreManager) | 1. Open login screen. 2. Enter valid StoreManager email & password. 3. Click Login.              | User is authenticated. Store dashboard is displayed.                                                                                | Active StoreManager account exists.            |
| AUTH-04      | Login succeeds with Remember Me = true               | 1. Open login screen. 2. Enter valid credentials. 3. Check "Remember me". 4. Click Login.        | User is authenticated. Long-lived refresh token is issued.                                                                          | Active account exists.                         |
| AUTH-05      | Login succeeds with Remember Me = false              | 1. Open login screen. 2. Enter valid credentials. 3. Uncheck "Remember me". 4. Click Login.      | User is authenticated. Short-lived refresh token is issued.                                                                         | Active account exists.                         |
| AUTH-06      | Login fails with wrong password                      | 1. Open login screen. 2. Enter valid email but wrong password. 3. Click Login.                   | Login is rejected. Error message shown on password field. HTTP 401, errorCode = InvalidCredentials.                                 | Active account exists.                         |
| AUTH-07      | Login fails with non-existent email                  | 1. Open login screen. 2. Enter email that does not exist. 3. Enter any password. 4. Click Login. | Login is rejected. Error shown on password field. HTTP 401, errorCode = InvalidCredentials.                                         | —                                              |
| AUTH-08      | Login fails with empty email                         | 1. Open login screen. 2. Leave email blank. 3. Enter any password. 4. Click Login.               | Form validation error: "Please input your email!" shown. API not called.                                                            | —                                              |
| AUTH-09      | Login fails with invalid email format                | 1. Open login screen. 2. Enter "notanemail" in email field. 3. Click Login.                      | Form validation error: "Please enter a valid email!" shown. API not called.                                                         | —                                              |
| AUTH-10      | Login fails with empty password                      | 1. Open login screen. 2. Enter valid email. 3. Leave password blank. 4. Click Login.             | Form validation error: "Please input your password!" shown. API not called.                                                         | —                                              |
| AUTH-11      | Login fails with password shorter than 6 characters  | 1. Open login screen. 2. Enter valid email. 3. Enter "abc" as password. 4. Click Login.          | Form validation error: "Password must be at least 6 characters!" shown. API not called.                                             | —                                              |
| AUTH-12      | Login fails for non-CMS user (PlaybackDevice role)   | 1. Open login screen. 2. Enter credentials of a PlaybackDevice account. 3. Click Login.          | Login is rejected. Error shown on email field: "You do not have permission to access this system". HTTP 403, errorCode = Forbidden. | A PlaybackDevice account exists.               |
| AUTH-13      | Login button shows loading state while pending       | 1. Open login screen. 2. Enter valid credentials. 3. Click Login and observe button immediately. | Login button shows loading spinner while request is in progress.                                                                    | Active account exists, slow network simulated. |

---

## Session / Token

| Test Case ID | Test Case Description                                     | Test Case Procedure                                                                                                              | Expected Results                                                                                             | Pre-conditions                       |
| ------------ | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------ |
| AUTH-14      | Logout invalidates current session                        | 1. Login successfully. 2. Click "Logout".                                                                                        | Session is cleared. Access token is removed. User is redirected to login screen. HTTP 200: isSuccess = true. | User is logged in.                   |
| AUTH-15      | Logout with missing/invalid token is rejected             | 1. Call POST /api/auth/logout without Authorization header.                                                                      | HTTP 401. Session is not affected.                                                                           | —                                    |
| AUTH-16      | Refresh token renews session successfully                 | 1. Login with valid credentials. 2. Wait for or simulate access token expiry. 3. Call POST /api/auth/refresh-token.              | New access token is returned. expiresAt is updated. HTTP 200, isSuccess = true.                              | A valid refresh token cookie exists. |
| AUTH-17      | Refresh token fails with missing/expired refresh cookie   | 1. Call POST /api/auth/refresh-token without a valid refresh token cookie.                                                       | HTTP 401 returned. errorCode = TokenExpired or Unauthorized. No new token issued.                            | No valid refresh cookie.             |
| AUTH-18      | Expired access token cannot access protected API directly | 1. Obtain a valid access token. 2. Wait for it to expire. 3. Call GET /api/auth/profile with expired token (without refreshing). | HTTP 401 returned. Request is denied. errorCode = TokenExpired or Unauthorized.                              | An expired access token exists.      |
| AUTH-19      | Get Profile returns correct user data                     | 1. Login as SystemAdmin. 2. Call GET /api/auth/profile with valid token.                                                         | HTTP 200. Response contains correct email, userId, firstName, lastName, roles = [0].                         | User is logged in.                   |
| AUTH-20      | Get Profile fails when unauthenticated                    | 1. Call GET /api/auth/profile without Authorization header.                                                                      | HTTP 401 returned. Profile data is not returned.                                                             | —                                    |

---

## Change Password

| Test Case ID | Test Case Description                                                 | Test Case Procedure                                                                                                                                               | Expected Results                                                                        | Pre-conditions                                |
| ------------ | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------- |
| AUTH-21      | Change password succeeds with valid input                             | 1. Login. 2. Navigate to Change Password. 3. Enter correct currentPassword. 4. Enter valid newPassword (≥ 6 chars). 5. Enter matching confirmPassword. 6. Submit. | HTTP 200. isSuccess = true. Message: "Password changed successfully".                   | User is logged in and knows current password. |
| AUTH-22      | Change password fails with wrong current password                     | 1. Login. 2. Navigate to Change Password. 3. Enter incorrect currentPassword. 4. Enter valid newPassword & confirmPassword. 5. Submit.                            | Request is rejected. HTTP 400. errorCode = InvalidCredentials. Error message shown.     | User is logged in.                            |
| AUTH-23      | Change password fails when new password is too short                  | 1. Login. 2. Navigate to Change Password. 3. Enter correct currentPassword. 4. Enter "abc" as newPassword. 5. Submit.                                             | Form validation error: "Password must be at least 6 characters!" shown. API not called. | User is logged in.                            |
| AUTH-24      | Change password fails when confirmPassword does not match newPassword | 1. Login. 2. Enter correct currentPassword. 3. Enter valid newPassword. 4. Enter different value in confirmPassword. 5. Submit.                                   | Form validation error: "The two passwords do not match!" shown. API not called.         | User is logged in.                            |
| AUTH-25      | Change password fails with empty fields                               | 1. Login. 2. Navigate to Change Password. 3. Leave all fields empty. 4. Submit.                                                                                   | Form validation errors shown for all required fields. API not called.                   | User is logged in.                            |
| AUTH-26      | Change password fails when unauthenticated                            | 1. Call POST /api/auth/change-password without valid Authorization header.                                                                                        | HTTP 401 returned. Password is not changed.                                             | —                                             |

---

## Response Contract

| Test Case ID | Test Case Description                                           | Test Case Procedure                                        | Expected Results                                                                                                | Pre-conditions         |
| ------------ | --------------------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------- |
| AUTH-27      | Successful login response contains required AuthResponse fields | 1. Login with valid credentials. 2. Inspect response body. | Response has isSuccess = true. data contains: accessToken (string), expiresAt (ISO 8601 UTC), roles (number[]). | Active account exists. |
| AUTH-28      | Failed login response contains errorCode                        | 1. Login with wrong password. 2. Inspect response body.    | Response has isSuccess = false. errorCode = "InvalidCredentials". message is not empty. data is null/absent.    | Active account exists. |
| AUTH-29      | Get Profile response contains all ProfileResponse fields        | 1. Login. 2. GET /api/auth/profile. 3. Inspect response.   | data contains: email, userId, firstName, lastName, roles. phoneNumber and avatarPath may be null.               | User is logged in.     |
