# Test Cases – CAMS (Context-Aware Music System)

> **Module:** CAMS – Space Music Control, Playback, Override, Queue, Audio Mixer, AI Analysis
> **ID Format:** `[WEB]:CAMS-NN`
> **Roles:** BrandManager (BM), StoreManager (SM)
> **Columns:** Test Case ID | Test Case Description | Test Case Procedure | Expected Results | Pre-conditions

---

## Group 1 – Space State & Initial Load

| Test Case ID  | Test Case Description                                    | Test Case Procedure                                                                                      | Expected Results                                                                                                                                                                                      | Pre-conditions                                               |
| ------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| [WEB]:CAMS-01 | StoreManager views space music control page              | 1. Log in as StoreManager. 2. Navigate to Space Management. 3. Open the music control panel for a space. | The space state is loaded via `GET /api/cams/spaces/{spaceId}/state`. Current track name, playlist, playback status (playing/paused/stopped), queue items, volume, and override status are displayed. | SM with at least one space. Space has an active playlist.    |
| [WEB]:CAMS-02 | Space state shows "no playback" when nothing is playing  | 1. Open the music control panel for a space with no active playback.                                     | UI shows no current track. `hlsUrl` is null. Player shows a stopped/idle state. Queue may be empty.                                                                                                   | Space has no ongoing playback.                               |
| [WEB]:CAMS-03 | Space state shows "Preparing…" when transcode is pending | 1. Open music control panel for a space where a track is being transcoded.                               | `pendingQueueItemId` is set. UI displays a "⏳ Preparing…" or similar pending indicator. HLS player is not started yet.                                                                               | A track has been added but its HLS is not yet ready.         |
| [WEB]:CAMS-04 | Space state shows Manual Override indicator              | 1. Open music control for a space with an active manual override.                                        | `isManualOverride = true`. A visible indicator (e.g., badge or label) shows the space is in manual override mode. Override mode (Playlist or Mood) is displayed.                                      | A manual override was previously applied to the space.       |
| [WEB]:CAMS-05 | Space state shows AI-driven indicator when no override   | 1. Open music control for a space with AI scheduling active.                                             | `isManualOverride = false`. UI shows "AI-driven" or no override indicator.                                                                                                                            | Space has no active manual override.                         |
| [WEB]:CAMS-06 | Volume percentage is displayed correctly                 | 1. Open music control panel. 2. Observe the volume control.                                              | Volume slider/indicator shows the correct value matching `volumePercent` from the state (0–100).                                                                                                      | Space has a non-default volume setting.                      |
| [WEB]:CAMS-07 | Mute status is reflected in UI                           | 1. Open music control for a muted space.                                                                 | Mute icon/button shows active/muted state.                                                                                                                                                            | Space has `isMuted = true`.                                  |
| [WEB]:CAMS-08 | Space queue items are listed with correct status         | 1. Open music control panel and view the queue section.                                                  | Queue items display track names, their statuses (Pending, Playing, Played, Skipped), and source (AI or Manager). Playing item is highlighted.                                                         | Space has a queue with multiple items of different statuses. |
| [WEB]:CAMS-09 | AI Explainability panel shows mood and BPM info          | 1. Open a space control panel where AI has run analysis.                                                 | AI panel shows: current Mood name, BPM Min, BPM Max, BPM Target, and the triggered fuzzy rule name and reason.                                                                                        | AI has analyzed the space (analysis has been triggered).     |
| [WEB]:CAMS-10 | BPM fields hidden when not available                     | 1. Open space where AI ran but no BPM metadata is available.                                             | BPM fields show as blank, "N/A", or are hidden. Mood and rule text still shown if available.                                                                                                          | Space has analysis result without BPM data.                  |

---

## Group 2 – Override Playlist / Mood / Tracks

| Test Case ID  | Test Case Description                                                    | Test Case Procedure                                                                                                              | Expected Results                                                                                                                                                                                           | Pre-conditions                              |
| ------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| [WEB]:CAMS-11 | Open Override Music drawer                                               | 1. Log in as SM or BM. 2. In the space control panel, click "Override Music" or equivalent action.                               | The Override Music drawer opens with three tabs: Tracks, Playlists, Moods. A reason field (optional) and a "Clear existing queue" checkbox are visible.                                                    | SM or BM with access to the space.          |
| [WEB]:CAMS-12 | Override with a specific playlist                                        | 1. Open the Override Music drawer. 2. Select the "Playlists" tab. 3. Choose a playlist from the list. 4. Click "Apply Override". | API calls `POST /api/cams/spaces/{spaceId}/override` with `{ playlistId: "..." }`. Success toast shown. Space transitions to play from the overridden playlist. `isManualOverride = true` reflected in UI. | Active space. Playlists exist in the brand. |
| [WEB]:CAMS-13 | Override with a mood                                                     | 1. Open Override Music drawer. 2. Select the "Moods" tab. 3. Choose a mood. 4. Click "Apply Override".                           | API called with `{ moodId: "..." }`. Backend selects up to 20 matching tracks (filtered by BPM band). Music transitions. Manual override indicator shown.                                                  | Active space. Moods exist.                  |
| [WEB]:CAMS-14 | Override with specific tracks                                            | 1. Open Override Music drawer. 2. Select the "Tracks" tab. 3. Select one or more tracks. 4. Click "Apply Override".              | API called with `{ trackIds: ["...", "..."] }`. Selected tracks are prepended to the queue in the supplied order. Music transitions.                                                                       | Active space. Tracks exist.                 |
| [WEB]:CAMS-15 | Override with "Clear existing queue" enabled                             | 1. Open Override Music drawer. 2. Select a playlist. 3. Check "Clear existing queue". 4. Apply.                                  | API called with `isClearManagerSelectedQueues: true`. All existing queue items are cleared before the new tracks are added.                                                                                | Active space with a non-empty queue.        |
| [WEB]:CAMS-16 | Override with reason field                                               | 1. Open Override Music drawer. 2. Enter a reason ("Event: Grand Opening"). 3. Apply override.                                    | API called with `reason: "Event: Grand Opening"`. Reason is stored in the audit trail.                                                                                                                     | Any accessible space.                       |
| [WEB]:CAMS-17 | Reason field max 500 characters                                          | 1. Open Override drawer. 2. Type a reason exceeding 500 characters. 3. Submit.                                                   | Validation error shown. Form does not submit.                                                                                                                                                              | Override drawer open.                       |
| [WEB]:CAMS-18 | Cannot submit override without selecting a source                        | 1. Open Override drawer. 2. Do not select any track/playlist/mood. 3. Click "Apply".                                             | Validation error or button is disabled. Override is not submitted.                                                                                                                                         | Override drawer open.                       |
| [WEB]:CAMS-19 | Override fails when no valid tracks found (mood with no matching tracks) | 1. Select a mood with no tracks matching it. 2. Apply override.                                                                  | API returns 422 (`InvalidInput`). Error message: "No valid override tracks found". Override not applied.                                                                                                   | Mood exists but no tracks match it.         |
| [WEB]:CAMS-20 | Override fails for space not owned by user                               | 1. Log in as SM of Store X. 2. Call override API for a space in Store Y (different store).                                       | API returns 403 Forbidden. Override not applied.                                                                                                                                                           | SM account. Cross-store spaceId used.       |
| [WEB]:CAMS-21 | Override for non-existent space returns 404                              | 1. Call `POST /api/cams/spaces/{invalidId}/override` with valid payload.                                                         | API returns 404. `isSuccess: false`.                                                                                                                                                                       | Invalid spaceId.                            |

---

## Group 3 – Cancel Override

| Test Case ID  | Test Case Description                                   | Test Case Procedure                                                                  | Expected Results                                                                                                                                                                                             | Pre-conditions                                   |
| ------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ |
| [WEB]:CAMS-22 | Cancel active manual override                           | 1. Open space control panel with override active. 2. Click "Cancel Override" button. | Confirmation may appear. API calls `DELETE /api/cams/spaces/{spaceId}/override`. `isManualOverride` becomes false. Override indicator disappears. AI scheduling will resume on the next cycle (~60 seconds). | Space has an active manual override.             |
| [WEB]:CAMS-23 | Cancel override does not immediately stop current track | 1. Cancel override while a track is playing.                                         | Currently playing track continues until it ends. The override flag is cleared. AI will take over after the current track finishes.                                                                           | Track is actively playing under manual override. |
| [WEB]:CAMS-24 | Cancel override on space with no active override        | 1. Call `DELETE /api/cams/spaces/{spaceId}/override` when no override is active.     | API returns 200 (idempotent). No error. UI remains unchanged.                                                                                                                                                | Space has no override.                           |

---

## Group 4 – Playback Controls

| Test Case ID  | Test Case Description                            | Test Case Procedure                                                                                 | Expected Results                                                                                                                                           | Pre-conditions                                           |
| ------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| [WEB]:CAMS-25 | Pause playback                                   | 1. Open space control panel while a track is playing. 2. Click the Pause button.                    | API calls `POST /api/cams/spaces/{spaceId}/playback` with `{ command: 1 }`. `isPaused` becomes true. Play button shown. `pausePositionSeconds` is set.     | Track is currently playing.                              |
| [WEB]:CAMS-26 | Resume playback                                  | 1. Open space control panel while playback is paused. 2. Click the Play/Resume button.              | API called with `{ command: 2 }`. Playback resumes from `pausePositionSeconds`. `isPaused` becomes false.                                                  | Playback is paused.                                      |
| [WEB]:CAMS-27 | Skip to next track                               | 1. Open space control panel. 2. Click the Skip Next (⏭) button.                                    | API called with `{ command: 6 }`. Queue advances to the next pending item. Current track becomes Skipped/Played. New track starts playing.                 | At least one pending track exists after the current one. |
| [WEB]:CAMS-28 | Skip Next when last track in queue returns error | 1. Click Skip Next when the current track is the last item in the queue.                            | API returns 422. Error message shown: "No next track available". Current track continues (or stops if queue end behavior = Stop).                          | Current track is the last in queue.                      |
| [WEB]:CAMS-29 | Skip to previous track                           | 1. Click Skip Previous (⏮) button.                                                                 | API called with `{ command: 7 }`. If past threshold: switches to previous track. If at start of track: restarts current track.                             | At least one track is available.                         |
| [WEB]:CAMS-30 | Skip to specific track in queue                  | 1. View the queue. 2. Click a specific Pending track item to skip to it (or use "Play Now" action). | API called with `{ command: 8, targetTrackId: "..." }`. Playback jumps to the chosen track.                                                                | Queue has multiple pending items.                        |
| [WEB]:CAMS-31 | SkipToTrack without targetTrackId returns 400    | 1. Call `/playback` with `{ command: 8 }` and no `targetTrackId`.                                   | API returns 400 with validation error.                                                                                                                     | Any active space.                                        |
| [WEB]:CAMS-32 | Seek to absolute position                        | 1. Drag the seek bar to a new position (e.g., 1:30).                                                | API called with `{ command: 3, seekPositionSeconds: 90 }`. Playback jumps to the 1:30 mark.                                                                | Track is playing (not paused).                           |
| [WEB]:CAMS-33 | Seek forward                                     | 1. Click the seek-forward button (+10s or similar).                                                 | API called with `{ command: 4, seekPositionSeconds: <delta> }`. Delta is translated to absolute position server-side. Playback moves forward by the delta. | Track is playing.                                        |
| [WEB]:CAMS-34 | Seek backward                                    | 1. Click the seek-backward button (−10s or similar).                                                | API called with `{ command: 5, seekPositionSeconds: <delta> }`. Playback moves backward by the delta.                                                      | Track is playing.                                        |
| [WEB]:CAMS-35 | Invalid playback command value returns 400       | 1. Call `/playback` with `{ command: 0 }` or `{ command: 10 }`.                                     | API returns 400. `errorCode: "InvalidInput"`.                                                                                                              | Any active space.                                        |
| [WEB]:CAMS-36 | Playback command on inactive space returns 404   | 1. Call `/playback` for a space that doesn't exist or is inactive.                                  | API returns 404.                                                                                                                                           | Invalid or inactive spaceId.                             |

---

## Group 5 – Queue Management

| Test Case ID  | Test Case Description                               | Test Case Procedure                                                                                      | Expected Results                                                                                                                                                                | Pre-conditions                                      |
| ------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [WEB]:CAMS-37 | Open Add to Queue drawer                            | 1. Click "Add to Queue" or "+ Queue" button in space control.                                            | AddToQueueDrawer opens. Shows tabs: Tracks, Playlists. A mode selector (Play Now / Play Next / Add to Queue) and an optional reason field are visible.                          | SM or BM with access to space.                      |
| [WEB]:CAMS-38 | Add tracks to queue – Play Now mode                 | 1. Open Add to Queue drawer. 2. Select Tracks tab, choose 2 tracks. 3. Set mode = "Play Now". 4. Submit. | API calls `POST /api/cams/spaces/{spaceId}/queue/tracks` with `{ trackIds: [...], mode: 1 }`. If tracks are stream-ready, playback switches to the first new track immediately. | Tracks selected. Space is active.                   |
| [WEB]:CAMS-39 | Add tracks – Play Next mode                         | 1. Open Add to Queue drawer. 2. Select 2 tracks, mode = "Play Next". 3. Submit.                          | Items inserted after the currently playing item. Music continues, then plays new items next. Queue list shows the new items in position 2 and 3.                                | Track is currently playing.                         |
| [WEB]:CAMS-40 | Add tracks – Add to Queue mode                      | 1. Select tracks, mode = "Add to Queue". 2. Submit.                                                      | New tracks appended to the end of the pending queue. Existing order unchanged.                                                                                                  | Active queue exists.                                |
| [WEB]:CAMS-41 | Add playlist to queue                               | 1. Open Add to Queue drawer. 2. Select Playlists tab. 3. Choose a playlist. 4. Select mode. 5. Submit.   | API calls `POST /api/cams/spaces/{spaceId}/queue/playlist` with `{ playlistId: "...", mode: N }`. Playlist tracks are expanded and inserted per mode.                           | Playlist exists in the brand.                       |
| [WEB]:CAMS-42 | Add to queue with Clear existing queue checked      | 1. Add tracks with "Clear existing queue" option enabled.                                                | Existing pending items are removed before new tracks are inserted. Queue afterward contains only the newly added tracks.                                                        | Space has a non-empty queue.                        |
| [WEB]:CAMS-43 | View queue items in the space control panel         | 1. Open space control panel. 2. View the Queue section.                                                  | Queue displays items with: track name, status (Pending/Playing/Played/Skipped), source (AI/Manager). Playing item is visually highlighted.                                      | Space has a queue with multiple items.              |
| [WEB]:CAMS-44 | Reorder pending queue items via drag-and-drop       | 1. In the queue, drag a Pending item to a new position.                                                  | API calls `PATCH /api/cams/spaces/{spaceId}/queue/reorder` with the new ordering of `queueItemIds`. Queue display updates to reflect the new order.                             | Queue has at least 2 pending items.                 |
| [WEB]:CAMS-45 | Reordering a Playing or Skipped item is not allowed | 1. Attempt to drag a Playing or Skipped item in the queue.                                               | The item cannot be dragged (locked in place). Only Pending items support reordering.                                                                                            | Queue has a Playing/Skipped item.                   |
| [WEB]:CAMS-46 | Remove a specific item from the queue               | 1. Click the remove (×) button on a queue item. 2. Confirm if prompted.                                  | API calls `DELETE /api/cams/spaces/{spaceId}/queue` with `{ queueItemIds: ["..."] }`. The item disappears from the queue list.                                                  | Queue has at least one removable item.              |
| [WEB]:CAMS-47 | Clear entire queue                                  | 1. Click "Clear Queue" or "Clear All" button. 2. Confirm in the dialog.                                  | API calls `DELETE /api/cams/spaces/{spaceId}/queue/all`. All queue items are removed. Playback stops. `SpaceStateSync` broadcast received.                                      | Space has a non-empty queue.                        |
| [WEB]:CAMS-48 | Clear queue shows confirmation before proceeding    | 1. Click "Clear Queue".                                                                                  | A confirmation dialog appears warning that playback will stop. "Confirm" and "Cancel" buttons shown.                                                                            | Queue is not empty.                                 |
| [WEB]:CAMS-49 | Cancel clear queue preserves queue                  | 1. Click "Clear Queue". 2. Click "Cancel" in the confirmation dialog.                                    | Queue remains unchanged. No API call made.                                                                                                                                      | Confirmation dialog is open.                        |
| [WEB]:CAMS-50 | Queue end behavior: Stop                            | 1. Set queue end behavior to "Stop". 2. Let the last track in the queue finish.                          | Playback stops after the last track. Space transitions to idle/stopped state.                                                                                                   | Queue has one remaining track. End behavior = Stop. |
| [WEB]:CAMS-51 | Queue end behavior: Repeat Queue                    | 1. Set queue end behavior to "Repeat Queue". 2. Let the last track finish.                               | Queue resets and starts looping from the beginning. All Played/Skipped items become Pending again (or a replay is triggered).                                                   | End behavior = RepeatQueue.                         |
| [WEB]:CAMS-52 | Queue end behavior: Return to Schedule              | 1. Set queue end behavior to "Return to Schedule". 2. Let last track finish.                             | AI scheduling is re-enabled. Space transitions back to AI-driven music after the queue is exhausted.                                                                            | End behavior = ReturnToSchedule.                    |

---

## Group 6 – Audio Mixer Controls

| Test Case ID  | Test Case Description                                    | Test Case Procedure                                                        | Expected Results                                                                                                                                                       | Pre-conditions               |
| ------------- | -------------------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| [WEB]:CAMS-53 | Adjust volume via slider                                 | 1. Open space control panel. 2. Drag the volume slider to 70.              | API calls `PATCH /api/cams/spaces/{spaceId}/state/audio` with `{ volumePercent: 70 }`. Volume indicator updates to 70. `SpaceStateSync` broadcast reflects new volume. | Space control panel is open. |
| [WEB]:CAMS-54 | Set volume to 0 (minimum)                                | 1. Drag the volume slider to 0.                                            | API called with `{ volumePercent: 0 }`. Volume shows 0. Music output volume sent to device as 0.                                                                       | Space control panel open.    |
| [WEB]:CAMS-55 | Set volume to 100 (maximum)                              | 1. Drag the volume slider to 100.                                          | API called with `{ volumePercent: 100 }`. Volume shows 100.                                                                                                            | Space control panel open.    |
| [WEB]:CAMS-56 | Volume outside 0–100 range rejected                      | 1. Call `PATCH /state/audio` with `{ volumePercent: 101 }`.                | API returns 400. `isSuccess: false`. Validation error returned.                                                                                                        | API call with invalid value. |
| [WEB]:CAMS-57 | Toggle mute ON                                           | 1. Click the mute button when space is unmuted.                            | API called with `{ isMuted: true }`. Mute icon shows active state. `isMuted` in space state becomes true.                                                              | Space is not muted.          |
| [WEB]:CAMS-58 | Toggle mute OFF                                          | 1. Click the mute button when space is muted.                              | API called with `{ isMuted: false }`. Mute deactivated. `isMuted` becomes false.                                                                                       | Space is muted.              |
| [WEB]:CAMS-59 | Change queue end behavior to Repeat Queue                | 1. Open audio mixer controls. 2. Set queue end behavior to "Repeat Queue". | API called with `{ queueEndBehavior: 1 }`. State updated. Queue end behavior indicator shows "Repeat Queue".                                                           | Space control open.          |
| [WEB]:CAMS-60 | Change queue end behavior to Return to Schedule          | 1. Set queue end behavior to "Return to Schedule".                         | API called with `{ queueEndBehavior: 2 }`. State updated accordingly.                                                                                                  | Space control open.          |
| [WEB]:CAMS-61 | Change queue end behavior to Stop                        | 1. Set queue end behavior to "Stop".                                       | API called with `{ queueEndBehavior: 0 }`.                                                                                                                             | Space control open.          |
| [WEB]:CAMS-62 | Audio state update does not trigger PlaybackStateChanged | 1. Update volume via the audio mixer. 2. Monitor SignalR events.           | Only `SpaceStateSync` is broadcast. `PlaybackStateChanged` is NOT emitted for audio-only state changes.                                                                | SignalR listener connected.  |
| [WEB]:CAMS-63 | Audio mixer requires at least one field                  | 1. Call `PATCH /state/audio` with an empty body `{}`.                      | API returns 400. Validation error: at least one field required.                                                                                                        | Valid auth for the space.    |

---

## Group 7 – Real-Time Sync (SignalR)

| Test Case ID  | Test Case Description                                        | Test Case Procedure                                                                        | Expected Results                                                                                                                                                                                | Pre-conditions                           |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| [WEB]:CAMS-64 | Manager UI updates after override via SignalR                | 1. Apply a playlist override. 2. Observe the space control UI without manually refreshing. | UI reflects the new current track, queue, and `isManualOverride = true` automatically via `SpaceStateSync` event. No page refresh needed.                                                       | SM/BM with space control open.           |
| [WEB]:CAMS-65 | Manager UI updates after playback command via SignalR        | 1. Press Pause in the UI.                                                                  | `PlaybackStateChanged` event received. `SpaceStateSync` also received. UI instantly shows paused state (play button appears).                                                                   | Track is playing.                        |
| [WEB]:CAMS-66 | Manager UI updates after queue change via SignalR            | 1. Add a track to the queue.                                                               | `SpaceStateSync` received with updated queue. Queue list in UI reflects the new item without manual refresh.                                                                                    | Space control panel open.                |
| [WEB]:CAMS-67 | Manager UI updates after audio state change                  | 1. Change volume.                                                                          | `SpaceStateSync` event received. Volume slider in UI updates.                                                                                                                                   | Space control panel open.                |
| [WEB]:CAMS-68 | SpaceStateSync event carries full state                      | 1. Trigger any CAMS operation. 2. Inspect the `SpaceStateSync` payload.                    | Payload contains: `spaceId`, `currentQueueItemId`, `hlsUrl`, `isPaused`, `isManualOverride`, `volumePercent`, `isMuted`, `queueEndBehavior`, `spaceQueueItems[]`, and AI explainability fields. | Connected to SignalR hub.                |
| [WEB]:CAMS-69 | seekOffsetSeconds is null in SignalR event                   | 1. Trigger a state sync and inspect the `SpaceStateSync` payload.                          | `seekOffsetSeconds` is `null` in the SignalR payload. Client must calculate offset from `startedAtUtc` if needed.                                                                               | Override or playback command triggered.  |
| [WEB]:CAMS-70 | seekOffsetSeconds is populated in REST GET /state            | 1. Call `GET /api/cams/spaces/{spaceId}/state` when a track is playing.                    | Response includes a non-null `seekOffsetSeconds` value (server-calculated).                                                                                                                     | Track is actively playing.               |
| [WEB]:CAMS-71 | StopPlayback event received when queue cleared               | 1. Clear the entire queue.                                                                 | SignalR emits `StopPlayback` to all connected clients for that space. Tablet application stops HLS playback.                                                                                    | Space has an active queue being cleared. |
| [WEB]:CAMS-72 | PlayStream event received after override with transitionType | 1. Apply an override. 2. Inspect the `PlayStream` event.                                   | `PlayStream` payload contains: `spaceId`, `hlsUrl`, `transitionType` (1=Immediate or 3=Pending), `isManualOverride: true`, `startedAtUtc`.                                                      | Override applied to a space.             |
| [WEB]:CAMS-73 | TransitionType 3 (Pending) shown when transcode not ready    | 1. Add a track that requires transcoding. 2. Monitor `PlayStream` event.                   | `transitionType = 3`. UI shows "⏳ Preparing…" state. A new `PlayStream` event with `transitionType = 1` follows once transcode completes.                                                      | Track without HLS URL added to queue.    |

---

## Group 8 – AI Analysis (Trigger Analysis)

| Test Case ID  | Test Case Description                          | Test Case Procedure                                                                                   | Expected Results                                                                                                                                                | Pre-conditions                    |
| ------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| [WEB]:CAMS-74 | BrandManager triggers AI analysis manually     | 1. Log in as BrandManager. 2. Navigate to space control. 3. Click "Trigger Analysis" (debug feature). | API calls `POST /api/cams/trigger-analysis/{spaceId}` with context data. Response shows: targetMood, BPM min/max/target, triggered rule, reason, analyzedAtUtc. | BM has access to the space.       |
| [WEB]:CAMS-75 | AI analysis result shows Target Mood           | 1. Trigger analysis. 2. View the result.                                                              | `targetMood` enum value (e.g., 0=Chill, 1=Focus, 2=Energetic) is shown with its human-readable name. `moodChanged` flag shown.                                  | Analysis triggered successfully.  |
| [WEB]:CAMS-76 | AI analysis result shows BPM recommendation    | 1. Trigger analysis for a space with IoT data.                                                        | Result shows `recommendedBpmMin`, `recommendedBpmMax`, `recommendedBpmTarget`.                                                                                  | Analysis with BPM-capable result. |
| [WEB]:CAMS-77 | AI analysis shows triggered rule and reason    | 1. Trigger analysis.                                                                                  | Result shows `triggeredRule` (e.g., "RULE_1_RUSH_HOUR") and a human-readable `reason` string.                                                                   | Analysis triggered.               |
| [WEB]:CAMS-78 | AI explainability panel updated after analysis | 1. Trigger analysis. 2. View AIExplainabilityPanel in space control.                                  | Panel updates to show the latest analyzed mood, BPM band, rule, and reason.                                                                                     | Analysis returned a result.       |
| [WEB]:CAMS-79 | AI analysis result shows applicable timestamps | 1. Inspect the analysis result.                                                                       | `analyzedAtUtc` shows the timestamp of the analysis (ISO 8601 UTC format).                                                                                      | Analysis completed.               |

---

## Group 9 – Role-Based Access Control (RBAC)

| Test Case ID  | Test Case Description                                  | Test Case Procedure                                                                  | Expected Results                                                                                                                 | Pre-conditions                              |
| ------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| [WEB]:CAMS-80 | BrandManager can control music in own brand's spaces   | 1. Log in as BrandManager. 2. Apply override to a space belonging to own brand.      | Override applied successfully. No 403 error.                                                                                     | BM's brand has accessible spaces.           |
| [WEB]:CAMS-81 | BrandManager cannot control a space in another brand   | 1. Call any CAMS endpoint with a spaceId belonging to a different brand.             | API returns 403 Forbidden.                                                                                                       | BM account. SpaceId from a different brand. |
| [WEB]:CAMS-82 | StoreManager can control music in own store's spaces   | 1. Log in as StoreManager. 2. Apply override to a space in own store.                | Override applied successfully.                                                                                                   | SM's store has accessible spaces.           |
| [WEB]:CAMS-83 | StoreManager cannot control a space in another store   | 1. Call any CAMS endpoint with a spaceId from a different store.                     | API returns 403 Forbidden.                                                                                                       | SM account. SpaceId from different store.   |
| [WEB]:CAMS-84 | SystemAdmin cannot perform CAMS operations             | 1. Log in as SystemAdmin. 2. Attempt to call override, playback, or queue endpoints. | API returns 403 or the UI does not expose CAMS control actions to SystemAdmin.                                                   | SystemAdmin account.                        |
| [WEB]:CAMS-85 | Unauthenticated request returns 401                    | 1. Call any CAMS endpoint without an Authorization header.                           | API returns 401 Unauthorized.                                                                                                    | No auth token provided.                     |
| [WEB]:CAMS-86 | CAMS features not available in UI for SystemAdmin role | 1. Log in as SystemAdmin. 2. Navigate to Space Management or any store page.         | No "Override Music", "Control Playback", "Add to Queue", or "Pair Device" actions visible. CAMS management UI is not accessible. | SystemAdmin account.                        |

---

## Group 10 – API Response Contract

| Test Case ID  | Test Case Description                                          | Test Case Procedure                                                  | Expected Results                                                                                                                                                                                                                                                                                                    | Pre-conditions                  |
| ------------- | -------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| [WEB]:CAMS-87 | GET /api/cams/spaces/{spaceId}/state returns full state object | 1. Call `GET /api/cams/spaces/{spaceId}/state`.                      | Response: `{ isSuccess: true, data: { spaceId, currentQueueItemId, hlsUrl, currentTrackName, moodName, isManualOverride, overrideMode, isPaused, pausePositionSeconds, seekOffsetSeconds, volumePercent, isMuted, queueEndBehavior, spaceQueueItems: [...], bpmMin, bpmMax, bpmTarget, fuzzyRule, fuzzyReason } }`. | Valid auth. Active space.       |
| [WEB]:CAMS-88 | POST override returns 200 with spaceId                         | 1. Apply a valid override.                                           | Response: `{ isSuccess: true, data: { spaceId: "..." } }` or `{ isSuccess: true }`. Full state update comes via SignalR `SpaceStateSync`.                                                                                                                                                                           | Valid payload. Authorized user. |
| [WEB]:CAMS-89 | POST playback control returns 200                              | 1. Send a valid playback command.                                    | Response: `{ isSuccess: true }`. SignalR delivers `PlaybackStateChanged` + `SpaceStateSync`.                                                                                                                                                                                                                        | Track playing. Valid command.   |
| [WEB]:CAMS-90 | PATCH audio state returns 200                                  | 1. Send a valid audio state update.                                  | Response: `{ isSuccess: true }`. `SpaceStateSync` broadcast.                                                                                                                                                                                                                                                        | Valid auth.                     |
| [WEB]:CAMS-91 | DELETE override returns 200                                    | 1. Cancel an active override.                                        | Response: `{ isSuccess: true }`. `SpaceStateSync` shows `isManualOverride: false`.                                                                                                                                                                                                                                  | Active override on space.       |
| [WEB]:CAMS-92 | Validation error returns 400 with errors array                 | 1. Submit an override with no source selected.                       | Response: `{ isSuccess: false, errorCode: "InvalidInput", errors: [{ field: "...", message: "..." }] }`.                                                                                                                                                                                                            | Invalid payload.                |
| [WEB]:CAMS-93 | No tracks found for override returns 422                       | 1. Override with a mood that has no matching tracks.                 | Response: `{ isSuccess: false, errorCode: "InvalidInput", message: "No valid override tracks found" }`. HTTP 422.                                                                                                                                                                                                   | Mood with no associated tracks. |
| [WEB]:CAMS-94 | Queue items in GET /state have correct structure               | 1. Fetch the space state. 2. Inspect each item in `spaceQueueItems`. | Each item has: `queueItemId`, `trackId`, `trackName`, `position`, `queueStatus` (0–3), `source` (0=AI, 1=Manager), `hlsUrl`, `isReadyToStream`.                                                                                                                                                                     | Queue is non-empty.             |
| [WEB]:CAMS-95 | Trigger analysis response has correct structure                | 1. Call `POST /api/cams/trigger-analysis/{spaceId}`.                 | Response: `{ isSuccess: true, data: { targetMood, recommendedBpmTarget, recommendedBpmMin, recommendedBpmMax, moodChanged, previousMood, triggeredRule, reason, analyzedAtUtc } }`.                                                                                                                                 | Valid auth. SpaceId accessible. |
